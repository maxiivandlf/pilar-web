import React from 'react';
import Button from '@mui/material/Button';

function ButtonPrimary({ titleButton }) {
  return <Button variant='contained'>{titleButton}</Button>;
}

export default ButtonPrimary;
